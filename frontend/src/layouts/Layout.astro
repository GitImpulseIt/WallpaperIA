---
import { getTranslation } from '../i18n';
import iconImg from '../img/icon.png';
import '../styles/categories-carousel.css';
import '../styles/screenshots-3d.css';
import CMPScript from '../components/CMPScript.astro';
import CategoriesCarouselScript from '../components/CategoriesCarouselScript.astro';
import Screenshots3DScript from '../components/Screenshots3DScript.astro';

interface Props {
  lang: string;
}

const { lang } = Astro.props;
const t = getTranslation(lang);
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={t.meta.description} />
    <meta name="generator" content={Astro.generator} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={t.meta.title} />
    <meta property="og:description" content={t.meta.description} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={t.meta.title} />
    <meta name="twitter:description" content={t.meta.description} />

    <link rel="icon" type="image/png" href={iconImg.src} />
    <title>{t.meta.title}</title>

    <!-- Consent Management Platform -->
    <CMPScript />

    <!-- jQuery -->
    <script is:inline src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  </head>
  <body>
    <slot />

    <!-- Modal plein écran pour les catégories -->
    <div class="fullscreen-modal" id="fullscreenModal">
      <div class="fullscreen-loader">
        <div class="spinner"></div>
        <p>{t.categories.imageLoading}</p>
      </div>
      <img src="" alt="" id="fullscreenImage" style="display: none;">
    </div>

    <!-- Scripts des carrousels (chargés après jQuery) -->
    <CategoriesCarouselScript />
    <Screenshots3DScript />
  </body>
</html>

<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
      'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
      sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    background: #0f172a;
    color: white;
    line-height: 1.6;
  }

  img {
    max-width: 100%;
    height: auto;
  }

  /* Scrollbar Styling */
  ::-webkit-scrollbar {
    width: 12px;
  }

  ::-webkit-scrollbar-track {
    background: #0f172a;
  }

  ::-webkit-scrollbar-thumb {
    background: #2196F3;
    border-radius: 6px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: #1976D2;
  }

  /* Selection */
  ::selection {
    background: rgba(33, 150, 243, 0.3);
    color: white;
  }

  /* Utilities */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Modal plein écran */
  .fullscreen-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.95);
    z-index: 9999;
    cursor: pointer;
    animation: fadeIn 0.3s ease-in-out;
  }

  .fullscreen-modal.active {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .fullscreen-loader {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    color: white;
  }

  .fullscreen-loader p {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .fullscreen-modal img {
    max-width: 95%;
    max-height: 95%;
    object-fit: contain;
    animation: zoomIn 0.3s ease-in-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes zoomIn {
    from {
      transform: scale(0.8);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }
</style>
